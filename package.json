{
  "name": "paper-explainer-dev",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "node --test tests/**/*.test.mjs",
    "test:runtime": "node --test tests/runtime/**/*.test.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
