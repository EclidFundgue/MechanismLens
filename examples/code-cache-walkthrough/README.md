# Code cache walkthrough

This fixture demonstrates the Code Lens path: Code IR records the selected source facts, Mechanism IR turns them into a cache-miss scenario, and Visual Intent binds each visual step to mechanism steps. The compiler then derives the Code Spotlight evidence.

The unresolved concurrency note is intentional: the selected source does not establish whether concurrent misses duplicate work.
